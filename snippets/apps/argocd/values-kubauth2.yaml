
dex:
  enabled: false

configs:
  cm:
    url: "https://argocd.ingress.kubo2.mbp"
    oidc.config: |
      name: Kubauth
      issuer: https://kubauth.ingress.kubo2.mbp
      clientID: argocd
      clientSecret: $oidc.kubauth.clientSecret
      requestedScopes: ["openid", "profile", "email", "groups"]
      enablePKCEAuthentication: true
      # logoutURL: https://kubauth.ingress.kubo2.mbp/oauth2/logout
      rootCA: $oidc.kubauth.rootCA
  rbac:
    policy.csv: |
      g, argocd-admin, role:admin